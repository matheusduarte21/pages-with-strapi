{
  "collectionName": "components_section_section_metadata",
  "info": {
    "displayName": "section_metadata",
    "icon": "apps"
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "section_id": {
      "type": "string",
      "required": true,
      "minLength": 3,
      "maxLength": 50,
      "regex": "^[a-zA-Z0]+([0-9-]*[a-zA-Z]+)*$"
    },
    "background": {
      "type": "boolean",
      "default": false,
      "required": true
    }
  }
}
